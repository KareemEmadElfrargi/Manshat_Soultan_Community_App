<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <ImageView
            android:id="@+id/iv_wait_icon"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:src="@drawable/wait"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_marginTop="8dp"
            app:layout_constraintStart_toStartOf="@id/iv_wait_icon"
            app:layout_constraintEnd_toEndOf="@id/iv_wait_icon"
            app:layout_constraintTop_toBottomOf="@id/iv_wait_icon"
            android:text="ترقب اضافة اعلانات القرية ⏳ قريبا"
            android:textStyle="bold"
            android:fontFamily="@font/base_font_cairo"
            android:textSize="@dimen/_18sdp"
            android:textColor="@color/title"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>